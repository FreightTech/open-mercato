/**
 * E2E Test Fixtures
 *
 * This module re-exports all fixtures for easy importing in tests.
 *
 * Usage:
 *   import { test, expect } from './fixtures'
 *   // or from a test file:
 *   import { test, expect } from '../fixtures'
 */
export { test, expect, TEST_USERS } from './auth.fixture'
