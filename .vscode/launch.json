{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: yarn dev",
            "type": "node-terminal",
            "request": "launch",
            "command": "source ~/.bashrc && nvm use default 24.13.0 && yarn dev",
            "console": "integratedTerminal"
        }
    ]
}