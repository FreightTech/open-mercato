import type { ModuleCli } from '@open-mercato/shared/modules/registry'

// CLI commands for shipment_tracking module
// Note: seed-carriers command removed as it depends on gitignored seed-carrier-configs.ts
// Use the setup.ts seedDefaults flow instead, or create the file locally for manual seeding.

export default [] as ModuleCli[]
